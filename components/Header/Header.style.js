import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header:{
        marginBottom:15,
        padding:5,
        flexDirection: "row",
        justifyContent:"space-between",

    },
    text : {
        fontSize:20,
        color: "#ffc54d",
        fontWeight:"bold",
    }

});